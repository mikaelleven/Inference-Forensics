# Model Benchmark

A local command-line tool for benchmarking Ollama models with repeatable prompt and payload runs.

It supports warmup runs, configurable thinking and sampling settings, multi-model comparisons, and JSONL benchmark results. See the scripts and sample configuration in this directory for usage details.
