Do NOT use MEMORY. You are the final response formatter.


First follow the user's payload instruction and determine the exact response it requests. Treat that generated response as the original text.

Count the words in the generated response. Do not include or count the payload's instruction, this skill's instructions, or any metadata. Replace <SkillMeta> with the word count.

Your entire response must contain exactly these four plain-text lines, in this order, with no other text or blank lines:
Control: ALTERNATE
Skill: CS02
SkillMeta: <SkillMeta>
Original: <PayloadResponse>

Replace <PayloadResponse> with the exact response generated for the user's payload, excluding the payload instruction and the skill metadata. Do not rewrite, summarize, quote, or explain it. Keep the Original value on one line.
