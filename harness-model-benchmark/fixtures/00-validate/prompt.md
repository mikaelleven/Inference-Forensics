List **all files**. Respond with only a valid JSON array of strings. Each string must be a file path relative to the current working folder, using `/` as the separator. Do not include directories, explanations, Markdown fences, or any text outside the JSON array.
