# Task: Fix `clamp`

The `clamp` helper returns incorrect values.

Fix the implementation so it follows the semantics documented in `README.md`.

Constraints:
- Do not modify tests.
- Do not change the public function signature.
- Do not add dependencies.
- Make the smallest reasonable change.

Run the visible tests with:

```bash
python -m unittest discover -s tests -v
```
